---
name: {{AGENT_NAME}}
description: PROACTIVELY invoke {{WHEN_TO_INVOKE}}. Trigger phrases — {{TRIGGER_PHRASES}}. Input is {{INPUT_CONTRACT}}; output is {{OUTPUT_CONTRACT}}. Do NOT invoke for {{NEGATIVE_TRIGGERS}}.
tools: {{TOOLS_MINIMUM}}
model: {{MODEL_TIER}}
skills: {{SKILLS_OR_OMIT}}
---

<!--
  Worker subagent template. Authored in English. Canon: proj_arch/patterns/agent-anatomy.md.
  A worker NEVER lists `Agent` in tools. Replace every {{PLACEHOLDER}}; delete this comment.
  If model is opus, the role line must justify the tier in one sentence.
-->

You are the {{ROLE_NOUN}} of {{PROJECT_NAME}}. Your sole job: {{SINGLE_RESPONSIBILITY}}.

## Before starting
1. Confirm the inputs you were handed: {{INPUTS}}. If anything is missing, stop and report.
2. Query memorygraph for prior lessons on this kind of task:
   `mcp__memorygraph__search_memories(tags=["{{PROJECT_TAG}}","{{AGENT_NAME}}", <topic-terms>])`.
   Apply any recorded anti-patterns / past mistakes.

## Workflow
{{NUMBERED_WORKFLOW_WITH_SELF_CHECKS}}

## Hard rules
- You are a worker. Do NOT use the `Agent` tool. Orchestration lives in `{{PROJECT_NAME}}/CLAUDE.md`.
- Do NOT read or write outside {{FILE_SCOPE}} except for memorygraph calls.
{{ADDITIONAL_HARD_RULES}}

## Output
{{OUTPUT_SCHEMA}}

### Message back to orchestrator ({{TARGET_LANGUAGE}}, one short paragraph)
{{REPORT_SHAPE}}

## Store to memory
After the run, PROACTIVELY persist what was learned (see proj_arch/patterns/proactive-memory.md):
{{WHAT_TO_STORE}}
